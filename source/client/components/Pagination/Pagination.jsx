import React from 'react';
import { subscribe } from 'horizon-react';
import { createDoc } from 'horizon-react/lib/utils';

const Pagination = (props) => {
  return (
    <div></div>
  );
};

export default subscribe()(Pagination);
